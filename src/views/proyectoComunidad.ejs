<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="<%= host %>estilos2.css">
  <div style="text-align: center; text-align: center;"> <%- include('header') %></div>
</head>
<div>
  <!-- TABLA PARA LA CONTENIDO-->
  <body>
    <div>
      <% if( datos.length != 0) { %>
        <div style="font-size: 16px; text-align: center; background-color:#004D40; color: lemonchiffon;">Lista de Proyectos de la Comunidad <%= datos[0].comunidad.nombre %></div>
        <table class="table-container" border="1">
          <thead>
              <tr>
                  <th>#</th>
                  <th>Nombre del Proyecto</th>
                  <th>Comunidad</th>
                  <th>Poa</th>
                  <th>Costo</th>
                  <th>Empresa Ejecutora</th>
                  <th>Categoria</th>
                  <th>Estado del Proyecto</th>  
              </tr>
          </thead>
          <tbody>
              <% for( i=0; i < datos.length; i++){ %>
                <tr>
                    <td><%= i+1 %></td>
                    <td><%= datos[i].nombre %></td>
                    <td><%= datos[i].comunidad.nombre %></td>
                    <td><%= datos[i].poa.nombre %></td>
                    <td>Bs.<%= datos[i].poa.monto %></td>
                    <td><%= datos[i].empresa.nombre %></td>
                    <td><%= datos[i].categoria.nombre %></td>
                    <td><%= datos[i].estadoProyecto %></td>
                </tr>
              <% } %>
          </tbody>
        </table>
      <% } else {%>
        <div style="font-size: 10px;"> No existe Registros......</div>
      <% } %>
    </div>  
  </body>
</div> 
</html>